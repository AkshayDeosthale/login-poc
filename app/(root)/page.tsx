import React from "react";
import { createToken,getAlgoAccountBalance,getCreatedAssets,sendAsset } from "@/actions/authFormActions";

const RootPage = async () => {
  // console.log(await createToken("sdc","sd",2,3));
  // console.log(await getCreatedAssets());
  // console.log(await sendAsset(""));
  return <div>RootPage</div>;
};

export default RootPage;
